#!/bin/bash

# use base_init
source .base_init

# is use tools
if [ "$1" != "" ] && [ -x "${run_dir}/tools/$1.sh" ]; then
    source "${run_dir}/tools/$1.sh"
    exit;
fi

# exec command
docker-compose $@
